// import React
import React from 'react';

// define the component’s function
const Header = () => {
    return (
        <div>
            <h2>This is the header section.</h2>
        </div>
    );
}

// Make the Component accessible from outside the component file.
export default Header;